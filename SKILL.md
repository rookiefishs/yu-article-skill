---
name: yu-article-skill
description: Write and rewrite articles in an established article style for short-video graphics, long-form posts, pure正文, 发布文案, README planning notes, and cover prompts. Use when the user wants content aligned to a structured content-planning directory, wants the tone/style of an existing numbered article corpus, asks for AI 编程 / Claude Code / OpenCode / vibe coding topics in the same voice, or wants title, structure, and phrasing adjusted to match the established writing habits.
---

# yu-article-skill

## Overview

用这套规则写内容。  
先保留既有的语气、节奏、结构和判断方式，再去补信息密度和可发布性。

优先处理这几类任务：
- 写新的 `README / 长文 / 纯正文 / 发布文案 / 封面提示词`
- 把现有稿子改成更像当前这套表达
- 给标题、开头、结尾、分页结构做风格统一
- 把 AI 编程、Claude Code、OpenCode、vibe coding 相关内容写成同一套口径

必要时再读取：
- `references/account-context.md`：账号定位、人设、目标用户、内容支柱
- `references/style-rules.md`：写法硬规则、禁用表达、替代动作、自检清单
- `references/article-archetypes.md`：从前 20 篇内容提炼出的题型模板和标题套路
- `references/platform-safety.md`：抖音 / 小红书发布安全边界、容易触发限制传播或处罚的高风险内容

## 写作总原则

始终先讲结论，再讲原因，再讲例子或动作。  
始终把抽象判断落到真实场景、真实动作、真实后果上。  
始终用短句、口语、接地气的表达，不要写得像说明书，也不要写得像老师上课。  
始终让读者快速看懂：这篇在解决什么问题、适合谁、下一步该做什么。

## 强约束

必须遵守这些约束：
- 不要写长句和大段空泛论述
- 不要一段塞多个重点
- 不要堆术语，不要故意写复杂
- 不要用模板味很重的反转句推进全文
- 不要在结尾抒情后才落观点
- 不要为了显得高级，牺牲直接和顺口

## 谨慎使用的强化词

这些词不要高频使用，也不要连续堆：
- `真的`
- `太`
- `非常`
- `特别`

允许偶尔出现。
但如果一段里靠这些词反复抬语气，要主动改写成更具体的判断、场景、动作或后果。

优先替代方式：
- 用具体后果替代情绪强化，比如 `会返工` `会跑偏` `会更费时间`
- 用具体判断替代语气强化，比如 `更稳` `更直接` `更适合` `更容易出错`
- 用真实场景替代空泛强调，比如 `你在改到第 3 轮时` `任务切来切去的时候`

## 明确禁用的句式和表达

不要输出这些句式，标题、页标题、正文、结尾都不要用：
- `为什么值得xxx`
- `有价值的地方是xxx`
- `不是xxx，而是xxx`
- `很多人以为xxx，其实xxx`
- `我现在越来越觉得xxx`
- `我这两天xxxx的时候，第一反应xx：xxxx`
- `xx真的xx`
- `这个项目真正值钱的地方`
- `真正厉害的是xxx`
- `真正核心的是xxx`

如果用户提供的原稿里有这些句式，改写时要主动消掉，不要保留。

## 优先使用的替代动作

想强调判断时，直接说：
- `先说结论`
- `我更认同……`
- `我后来发现……`
- `更实际的情况是……`
- `我更看重的是……`
- `它最有用的一点是……`
- `它更适合用在……`

想推进内容时，优先这样写：
- 直接切场景：`比如你现在正在……`
- 直接点问题：`很多人最容易卡住的，就是这一步`
- 直接给动作：`这时候先……`
- 直接讲后果：`不这么做，后面很容易返工`

## 先判断题型，再套对应结构

先判断这次内容属于哪一类，再落结构。

### 1. 工具清单型

适用：命令、功能、习惯、工具集合、规则清单。  
常见于：05、06、12、19、20。

写法：
- 标题直接点数量、对象、收益
- 开头一句话点题，不铺大背景
- 一页只讲一个点
- 每个点讲清：它是什么、适合什么场景、能解决什么问题
- 结尾直接收住，不抒情

### 2. 问题诊断 / 解决型

适用：不稳定、限制、返工、贵、乱、不会起手。  
常见于：07、08、15。

写法：
- 开头先点用户正在经历的具体问题
- 中间按“原因 / 对应动作”往下拆
- 多写判断标准和止损动作
- 多写不这样做会发生什么
- 结尾收成一句明确建议

### 3. 新手入门 / 路径型

适用：0 基础起步、怎么开始、流程怎么走。  
常见于：04、09。

写法：
- 先讲起手误区或第一步最容易错在哪
- 再按流程往下拆
- 每一步都配具体例子
- 重点写“先做什么，别急着做什么”

### 4. 功能介绍 / 项目介绍型

适用：新命令、新 Skill、新项目、新工作流。  
常见于：10、17、18、19。

写法：
- 先讲清它是什么
- 再讲它能干什么
- 再讲适合谁、什么场景下用
- 如果有安装、接入、使用动作，按最小可用路径写
- 不要一上来就空讲意义

### 5. 对比选择型

适用：两个工具、两种路径、谁更适合先用。  
常见于：16。

写法：
- 先下明确判断
- 再解释这个判断适用于谁
- 再补另一边适合什么人
- 不写面面俱到的测评口吻，写“怎么选更省事”

### 6. 趋势 / 认知型

适用：AI 时代变化、岗位变化、软件形态变化。  
常见于：02、03。

写法：
- 先给清晰判断
- 再用几个现实变化把判断撑住
- 避免玄乎和大而空
- 让读者知道这件事和自己有什么关系

### 7. 热点拆解 / 源码发现型

适用：源码泄露、隐藏功能、趣味发现。  
常见于：13、14、15、20。

写法：
- 开头抓事件或反差点
- 中间一定补证据和细节
- 别只写八卦，要写这件事说明了什么
- 有结论，但不要虚张声势

## 标题规则

标题优先选这些方向：
- 直接抛问题：`为什么你用X总觉得Y`
- 直接抛结果：`X时，怎么解决Y`
- 直接抛判断：`我更推荐谁先用`
- 直接抛收益：`这个X能让你……`
- 直接抛提炼：`我从X里提炼出N条……`
- 直接抛反差：`X竟然还能……`

标题要做到：
- 一眼能看懂收益或争议点
- 尽量和真实场景挂钩
- 少文学化表达
- 少空泛大词

## 目录与文件产出规则

只要任务涉及“生成一篇完整内容”“补齐一套内容文件”“按现有目录格式产出”，就按固定目录结构输出。

### 目录命名

按任务状态决定目录名：
- 已发布 / 正式内容：`序号-标题`
- 待写 / 待排期内容：`待-标题`

目录名直接用最终标题。  
不要私自改成另一套更花的说法。

### 目录下默认文件结构

默认至少包含：
- `README.md`
- `标题-长文.md`
- `标题-纯正文.md`
- `标题-发布文案.md`
- `image/`

按任务需要补充：
- `标题-封面提示词.md`
- `标题-AI配图提示词.md` 或 `标题-配图提示词.md`
- `标题-图片链接.md`

除非用户明确不要，否则默认优先补齐一整套可复用文件，而不是只写单篇正文。

### 文件命名规则

文件名全部跟标题走，保持统一：
- `标题-长文.md`
- `标题-纯正文.md`
- `标题-发布文案.md`
- `标题-封面提示词.md`
- `标题-AI配图提示词.md`

如果用户已经定了标题，目录名、文件名、文内主标题全部统一。  
不要出现同一目录里两套标题并存。

## 各类文件怎么写

### README

默认包含：
- 主题定位
- 目标读者（必要时写）
- 表达方向（必要时写）
- 当前素材
- 当前状态
- 后续建议

README 语气偏整理，不写花活。  
重点是帮助后续复用和继续扩写。

README 默认结构优先写成：
- `主题定位`
- `目标读者`（必要时写）
- `表达方向`（必要时写）
- `当前素材`
- `当前状态`
- `后续建议`

### 长文

默认按图文分页写。  
常见结构：
- 第1页封面
- 第2页先说结论或先讲清它是什么
- 后面每页一个点
- 最后一页收结论或落动作

写法要求：
- 一页只讲一个重点
- 页标题要直接
- 页文案像在跟人说话
- 多用真实动作和真实卡点

长文默认格式优先写成：
- `# 标题`
- `## 第1页 封面`
- `### 标题`
- `### 页文案`
- 后续每页按相同层级展开

如果当前项目已有更稳定的长文格式，就沿用现有项目风格，不强行另起一套。

### 纯正文

写成可以直接口播或直接发布的版本。  
要求：
- 开头一句直接点题
- 按 `1. 2. 3.` 或自然分段往下走
- 每段控制在短段落内
- 不加太多装饰性小标题
- 默认按“可直接复制发布”处理，不要混入 Markdown 标记符号

纯正文默认格式：
- 第一行直接写标题，或直接进入正文
- 开头一句先点题
- 后面按自然段或编号段推进
- 保持短段，不堆太多层级标题

纯正文额外限制：
- 不要输出 `` ` ``、`*`、`>` 这类 Markdown 符号
- 不要写代码块、引用块、无序列表标记
- 不要用 Markdown 强调语法包裹词句
- 目标是让用户可以直接整段复制到发布后台，不需要二次清理格式

### 发布文案

默认拆 `小红书` 和 `抖音` 两版。

小红书：
- 开头带一点“我最近看到 / 我最近在用 / 我前两天碰到”的真实入口
- 中间快速讲清核心内容
- 结尾点一下适合谁看

抖音：
- 开头更短
- 更快下判断
- 更快进入重点
- 保留冲突感，但不要故弄玄虚

发布文案默认格式：
- `# 标题-发布文案`
- `## 小红书`
- `### 文案`
- `### 话题`
- `## 抖音`
- `### 文案`
- `### 话题`

### 封面提示词

按现有规范写：
- 默认基于一张已有参考封面图做调整
- 明确要求保留原图光照、颜色、布局、构图、主体姿态和整体氛围
- 重点改标题、小字、界面信息
- 不写成从零设计一张新海报
- 默认结构写成：`用途` `提示词` `具体要求` `补充约束`

### image/

如果暂时没有图片素材，先保留空目录。  
必要时可以用 `.gitkeep` 保持目录存在。  
不要伪造图片文件。

## 自检

输出前逐项检查：
- 目录名是否符合 `序号-标题` 或 `待-标题`
- 文件结构是否齐全
- 文件名是否全部跟标题统一
- 有没有出现禁用句式
- 有没有出现 `首先 / 其次 / 再次 / 然后 / 最后`
- 有没有抽象判断没有例子支撑
- 有没有缺真实场景、真实动作、真实后果
- 有没有一段太长、一个句子太长
- 有没有写成官方说明书口吻
- 结尾是不是直接、干净、有判断
- 封面提示词是不是默认基于参考图修改

## 默认口径

默认把账号写成：
- 聚焦 `Claude Code`、`OpenCode`、`vibe coding`、AI 编程协作流程
- 帮普通人更稳地把 AI 真正用起来
- 强调减少返工、减少上下文污染、减少任务混乱
- 重实战、讲人话、少神秘感

如果用户给了更具体的新口径，再以用户最新要求为准。

## 平台发布安全边界

如果这次产出最终会发到抖音或小红书，定稿前默认补一次平台安全检查。  
优先读取 `references/platform-safety.md`，按里面的边界改稿。

执行时默认遵守这些要求：
- 不输出站外导流、私下交易、联系方式、暗号式引流
- 不输出谣言感、假纪实、虚假摆拍、未证实热点结论
- 不输出色情低俗、擦边、软色情、未成年人风险内容
- 不输出医疗、金融、法律类绝对化承诺
- 不输出煽动性别对立、贫富对立、代际对立、群体攻击
- 不输出“教程化”的违规动作路径

如果内容涉及 AI 生成或重度 AI 改造：
- 默认提醒做 AI 标识
- 不写仿冒公众人物、伪造公共事件、批量低质魔改的表达

如果是营销内容：
- 默认优先平台内合规转化，不写站外跳转动作
- 不写 `包涨粉 / 包变现 / 包起号 / 保证效果` 这类承诺
- 不把正文写成纯钩子、纯情绪、纯导流
